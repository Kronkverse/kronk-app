<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	android:layout_width="match_parent"
	android:layout_height="wrap_content"
	android:paddingBottom="8dp">

	<LinearLayout
		android:id="@+id/card_root"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:layout_gravity="center_horizontal"
		android:maxWidth="400dp"
		android:orientation="vertical"
		android:elevation="3dp"
		android:clickable="true"
		android:focusable="true">

		<!-- Badge header: solid space-color background -->
		<LinearLayout
			android:id="@+id/badge_header"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:orientation="horizontal"
			android:gravity="center_vertical"
			android:paddingStart="14dp"
			android:paddingEnd="14dp"
			android:paddingTop="8dp"
			android:paddingBottom="8dp">

			<ImageView
				android:id="@+id/badge_icon"
				android:layout_width="14dp"
				android:layout_height="14dp"
				android:layout_marginEnd="6dp"
				android:importantForAccessibility="no"
				tools:src="@drawable/ic_help_24px"/>

			<TextView
				android:id="@+id/badge_label"
				android:layout_width="wrap_content"
				android:layout_height="wrap_content"
				android:textSize="12sp"
				android:textColor="#FFFFFF"
				android:textStyle="bold"
				android:letterSpacing="0.07"
				android:textAllCaps="true"
				tools:text="QUESTION"/>

		</LinearLayout>

		<!-- Body: question / answer / seed text -->
		<TextView
			android:id="@+id/card_body"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:paddingStart="14dp"
			android:paddingEnd="14dp"
			android:paddingTop="12dp"
			android:paddingBottom="12dp"
			android:textSize="14sp"
			android:textColor="?colorM3OnSurface"
			android:lineSpacingMultiplier="1.3"
			tools:text="What is your favorite color?"/>

		<!-- Footer: lock icon + answerer avatars + count + action -->
		<LinearLayout
			android:id="@+id/card_footer"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:orientation="horizontal"
			android:gravity="center_vertical"
			android:paddingStart="14dp"
			android:paddingEnd="14dp"
			android:paddingTop="10dp"
			android:paddingBottom="10dp"
			android:minHeight="44dp">

			<!-- Answerer avatars: up to 4, loaded dynamically with overlap -->
			<LinearLayout
				android:id="@+id/avatars_row"
				android:layout_width="wrap_content"
				android:layout_height="22dp"
				android:orientation="horizontal"
				android:clipChildren="false"
				android:clipToPadding="false"
				android:layout_marginEnd="8dp"
				android:visibility="gone">

				<ImageView android:id="@+id/avatar_0"
					android:layout_width="22dp" android:layout_height="22dp"
					android:importantForAccessibility="no"/>
				<ImageView android:id="@+id/avatar_1"
					android:layout_width="22dp" android:layout_height="22dp"
					android:layout_marginStart="-6dp"
					android:importantForAccessibility="no"/>
				<ImageView android:id="@+id/avatar_2"
					android:layout_width="22dp" android:layout_height="22dp"
					android:layout_marginStart="-6dp"
					android:importantForAccessibility="no"/>
				<ImageView android:id="@+id/avatar_3"
					android:layout_width="22dp" android:layout_height="22dp"
					android:layout_marginStart="-6dp"
					android:importantForAccessibility="no"/>

			</LinearLayout>

			<TextView
				android:id="@+id/count_text"
				android:layout_width="0dp"
				android:layout_height="wrap_content"
				android:layout_weight="1"
				android:textSize="13sp"
				android:textColor="?colorM3OnSurfaceVariant"
				tools:text="3 answers"/>

			<TextView
				android:id="@+id/see_answers_text"
				android:layout_width="wrap_content"
				android:layout_height="wrap_content"
				android:textSize="13sp"
				android:textStyle="bold"
				android:visibility="gone"
				tools:text="See all answers →"/>

		</LinearLayout>

	</LinearLayout>

</FrameLayout>
